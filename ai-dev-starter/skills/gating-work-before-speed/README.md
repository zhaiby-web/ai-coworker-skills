# 小白指挥AI开发之怎么开工

> Gating Work Before Speed — boundaries before velocity.

治"AI 一听'直接开干'就真的直接开干"：动手写代码前先过**五问**（用什么契约？什么数据源？改哪一层？怎么证明？不能做什么？），锁定正式仓库/分支/基线，**commit→push→建MR 逐级授权**（说了"顺手提交"也只授权到 commit 这一级）；改到共享代码立即暂停上报。与 TDD 等实现方法论是组合关系：门禁管"能不能开工、在哪开工"，方法论管"怎么写"。

## 何时用
- 让 AI 在 git 仓库里加功能、改代码、修 bug 之前
- 说了"直接开干/写完顺手提交"——这句话恰恰最需要把授权拆开
- AI 干着干着改到了公共组件/别人的代码

**触发例句**：「帮我加个功能，直接开干」「改这个 bug，写完提交」「它是不是乱改我代码了」

## 何时不用
- 纯阅读、分析、方案设计（不写实现代码就没有开工门禁）
- 明确标注"丢弃式"的沙盒实验（但产物仍不得混入正式仓库）

## 安装
SkillHub 页面直接安装，或拷贝本目录到 `~/.claude/skills/` 或 `~/.codex/skills/`。建议在全局指令钉常驻规则（见技能包主页），因为"顺手一改"的场景靠被动触发会漏。


> 兼容性：适配 Claude Code、Codex 及任何支持 SKILL.md 规范的平台。包内 `agents/openai.yaml` 仅为 Codex 侧的显示名配置文件，**不调用任何 OpenAI 接口、零网络依赖**。

## FAQ（来自作者自己踩过的坑）

**Q: 我说"直接开干"，它还要问五个问题，是不是太磨叽？**
A: 五问一次答完就全速推进，不再重复盘问。磨叽的反面作者亲历过：三个会话在不同假设下并行一周，最后统一返工——五个问题比一次返工便宜得多。

**Q: 代码写完了才发现放错仓库，怎么防？**
A: 开工前四句话：这段代码最终提交到哪个仓库？工作区干净吗？分支从确认过的基线拉的吗？提交范围混了别的东西吗？作者的血泪版本是：多轮 QA 都过了，最后发现整批代码写在了错误的仓库里。

**Q: 我让它提交，它只 commit 不 push，为什么？**
A: 三级授权链：允许 commit ≠ 允许 push ≠ 允许建 MR，每级单独授权。"顺手提交"里藏着的正是需要拆开的打包授权——这是防"顺手推上线"的最后闸门。
## 来源
源自真实事故：多个 AI 会话在不同假设下并行开发，多轮 QA 通过后才发现整批代码写进了错误的仓库——"验证路径悄悄变成了交付路径"。完整技能包：[github.com/zhaiby-web/ai-coworker-skills](https://github.com/zhaiby-web/ai-coworker-skills)
